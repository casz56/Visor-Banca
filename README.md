# Visor Banca (LIVE SERVER)

Este paquete está diseñado para abrirse directamente con:
- VS Code Live Server (127.0.0.1:5500)

## Uso
1) Abre la carpeta en VS Code
2) Click derecho en index.html -> Open with Live Server

## Nota
Esta versión NO usa React/Recharts para evitar build.
Si deseas la versión React original, usa el ZIP de Vite y ejecuta `npm run dev`.
